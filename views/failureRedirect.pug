doctype html
html(lang="en")
  head
    title SSO Error
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    link(rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css")
    style.
      body {
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        background-color: #f8f9fa;
      }
      .error-message {
        text-align: center;
        color: #dc3545;
      }
  body
    .container
      .card(text-center)
        .card-body
          h1.card-title.text-danger Unexpected Error
          p.card-text An unexpected error occurred during the SSO process. Please try again later.
        //-   a.btn.btn-primary(href="/login") Go to Login Page
